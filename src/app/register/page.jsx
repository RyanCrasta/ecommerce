import React from "react";
import { Register } from "../_components/Register";

export const metadata = {
  title: "Register",
  description: "Register page for ecommerce site",
};

export default function Page() {
  return <Register />;
}
